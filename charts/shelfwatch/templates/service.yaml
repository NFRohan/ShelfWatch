apiVersion: v1
kind: Service
metadata:
  name: {{ include "shelfwatch.fullname" . }}
  labels:
    {{- include "shelfwatch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shelfwatch.selectorLabels" . | nindent 4 }}

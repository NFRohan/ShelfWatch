# Prometheus config for ShelfWatch
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "shelfwatch-inference"
    metrics_path: /metrics
    static_configs:
      - targets: ["inference:8000"]
        labels:
          service: "shelfwatch"
          environment: "local"
